# ROS2 colcon build artifacts
build/
install/
log/

# Python
__pycache__/
*.pyc
.pytest_cache/

# Editors
.vscode/
.idea/

# Recorded data
rosbag2_*/
*.db3
*.mcap
*.mp4
*.zip

# ml/ pipeline data & training runs (large, regenerable -- see ml/README.md)
ml/data/
ml/runs/

# Untracked FastSAM experiment (unused template scripts + downloaded .pt
# checkpoint -- see conversation: shelved in favor of the SAM3 pipeline)
fastsam/

# Third-party RPLIDAR A1 driver -- clone per machine, not vendored:
#   cd src && git clone https://github.com/Slamtec/sllidar_ros2.git
#   colcon build --packages-select sllidar_ros2
src/sllidar_ros2/
