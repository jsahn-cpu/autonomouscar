# Reference paths

Place pre-recorded reference path files here (CSV or YAML).

Expected columns/fields (TODO: finalize): `x`, `y`, `yaw` per waypoint, in
the map/global frame.

TODO: add a sample path file once the format is finalized.
